# Disables a module

Make sure that a certain module is not used in all models

## Usage

``` r
disable_module(config_file, folder = ".", module)
```

## Arguments

- config_file:

  character; name of LakeEnsemblR_WQ config file

- folder:

  path; location of config_file

- module:

  character; name of the module to disable
