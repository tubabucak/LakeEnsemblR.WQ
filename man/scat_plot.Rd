% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scat_plot.r
\name{scat_plot}
\alias{scat_plot}
\title{Scatter plot modeled vs observed}
\usage{
scat_plot(
  temp_glm,
  temp_wet,
  temp_selma,
  temp_avg,
  temp_avg_pareto,
  temp_obs,
  y_title
)
}
\arguments{
\item{temp_glm}{Data frame: Modeled values from the GLM model in wide format with columns named "datetime" and "Depth_*".}

\item{temp_wet}{Data frame: Modeled values from the WET model in wide format with columns named "datetime" and "Depth_*".}

\item{temp_selma}{Data frame: Modeled values from the SELMAPROTBAS model in wide format with columns named "datetime" and "Depth_*".}

\item{temp_avg}{Data frame: Averaged modeled values across models in wide format with columns named "datetime" and "Depth_*".}

\item{temp_avg_pareto}{Data frame: Pareto-optimized averaged modeled values in wide format with columns named "datetime" and "Depth_*".}

\item{temp_obs}{Data frame: Observed values in wide format with columns named "datetime" and "Depth_*".}

\item{y_title}{Character: A string to label the y-axis (e.g., "Temperature (°C)", "DO (mg/L)").}
}
\value{
A list containing:
\itemize{
  \item A ggplot2 object with the observed vs predicted scatter plot and model statistics annotated.
  \item A named list of statistics (RMSE and KGE) for each model.
}
}
\description{
This function plots the observed versus predicted across depths and annotate KGE and RMSE on the graph
}
