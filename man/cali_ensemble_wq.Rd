% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cali_ensemble_wq.R
\name{cali_ensemble_wq}
\alias{cali_ensemble_wq}
\title{Calibrate an ensemble of LakeEnsemblR.WQ models}
\usage{
cali_ensemble_wq(
  models = c("GLM-AED2", "GOTM-WET", "GOTM-Selmaprotbas", "Simstrat-AED2"),
  calib_setup,
  yaml_file,
  folder = ".",
  model_dirs = NULL,
  param_names = NULL,
  model_filter = NULL,
  wq_config_file = NULL,
  n_samples = 50,
  yaml_file_model = "gotm.yaml",
  par_file = "simstrat.par",
  obs_file = NULL,
  obs_to_model_units = TRUE,
  spin_up_days = NULL,
  stats_by_depth = FALSE,
  return_best = TRUE,
  best_metric = "KGE",
  parallel = FALSE,
  n_workers = NULL,
  parallel_dir = tempdir(),
  keep_worker_dirs = FALSE,
  verbose = TRUE,
  on_error = c("skip", "stop"),
  save_results = FALSE,
  output_dir = NULL,
  output_prefix = "lhc_results",
  write_best = FALSE,
  write_target = c("par_file", "config"),
  config_file = NULL
)
}
\arguments{
\item{models}{Character vector of models to calibrate. Supported values are
\code{"GLM-AED2"}, \code{"GOTM-WET"}, \code{"GOTM-Selmaprotbas"}, and
\code{"Simstrat-AED2"}. Matching is case-insensitive.}

\item{calib_setup}{Either a data frame in \code{calib_setup} format or a
named list of such data frames. If a data frame is supplied and contains
\code{model_coupled}, rows are split by model.}

\item{yaml_file}{Character scalar, vector, or named list. Path(s) to metric
YAML file(s) passed to \code{run_lhc_wq()}.}

\item{folder}{Character. Base folder used to resolve relative paths and for
optional write-back operations.}

\item{model_dirs}{Character vector/list of model directories. Can be scalar
(reused), length \code{length(models)} (positional), or named by model.
Defaults to \code{file.path(folder, models)}.}

\item{param_names}{Optional. Either a character vector reused for all models,
or a named list/vector by model. If \code{NULL}, uses
\code{unique(calib_setup$pars)} per model.}

\item{model_filter}{Optional model filter(s) passed to \code{run_lhc_wq()}.
Accepts scalar, length-\code{length(models)} vector, or named vector/list.}

\item{wq_config_file}{Character scalar, vector, or named list of WQ config
path(s) passed to \code{run_lhc_wq()}.}

\item{n_samples}{Integer. Number of LHS samples per model.}

\item{yaml_file_model}{Character scalar/vector/list. GOTM YAML file name(s)
passed to \code{run_lhc_wq()}.}

\item{par_file}{Character scalar/vector/list. Simstrat \code{.par} file
name(s) passed to \code{run_lhc_wq()}.}

\item{obs_file}{Optional observed-data CSV path. When supplied, each model
returns the flattened stats data.frame from \code{run_lhc_wq()}.}

\item{obs_to_model_units}{Logical. Passed to \code{run_lhc_wq()}.}

\item{spin_up_days}{Numeric or \code{NULL}. Passed to \code{run_lhc_wq()}.}

\item{stats_by_depth}{Logical. Passed to \code{run_lhc_wq()}.}

\item{return_best}{Logical. Passed to \code{run_lhc_wq()}.}

\item{best_metric}{Character. Passed to \code{run_lhc_wq()}.}

\item{parallel}{Logical. Passed to \code{run_lhc_wq()}.}

\item{n_workers}{Integer or \code{NULL}. Passed to \code{run_lhc_wq()}.}

\item{parallel_dir}{Character. Passed to \code{run_lhc_wq()}.}

\item{keep_worker_dirs}{Logical. Passed to \code{run_lhc_wq()}.}

\item{verbose}{Logical. Print progress messages.}

\item{on_error}{Character. One of \code{"skip"} or \code{"stop"}.}

\item{save_results}{Logical. If \code{TRUE}, save each model result as RDS.}

\item{output_dir}{Character. Directory for saved RDS files when
\code{save_results = TRUE}. Defaults to \code{folder}.}

\item{output_prefix}{Character. Prefix for saved result files when
\code{save_results = TRUE}.}

\item{write_best}{Logical. If \code{TRUE}, call
\code{write_best_calib_to_par_files()} for each successful model.}

\item{write_target}{Character. Passed to
\code{write_best_calib_to_par_files()}.}

\item{config_file}{Character. Required when \code{write_best = TRUE}; path to
the WQ master config used by \code{write_best_calib_to_par_files()}.}
}
\value{
A list with:
\describe{
  \item{results}{Named list of per-model outputs from \code{run_lhc_wq()}.}
  \item{summary}{Data frame with one row per requested model and run status.}
  \item{best_parameter_sets}{Named list of per-model best summaries (or \code{NULL}).}
  \item{write_back}{Named list with best-write status per model (when enabled).}
}
}
\description{
High-level calibration wrapper inspired by LakeEnsemblR
\code{cali_ensemble()}, implemented for the LakeEnsemblR.WQ calibration
workflow. The function runs \code{run_lhc_wq()} model-by-model, using either
a combined \code{calib_setup} table (with \code{model_coupled}) or a named
list of per-model setup tables.
}
