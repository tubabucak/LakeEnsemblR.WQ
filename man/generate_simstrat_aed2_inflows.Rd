% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_simstrat_input.r
\name{generate_simstrat_aed2_inflows}
\alias{generate_simstrat_aed2_inflows}
\title{Generate Simstrat–AED2 inflow files for active modules}
\usage{
generate_simstrat_aed2_inflows(
  aed2_file,
  phyto_pars_file = NULL,
  sim_par,
  out_dir,
  levels = NULL,
  surf_flow = NULL,
  default_value = 0,
  unit = "millimolesPerMeterCubed",
  overwrite = FALSE
)
}
\arguments{
\item{aed2_file}{Character; path to \code{aed2.nml}.}

\item{phyto_pars_file}{Character; path to \code{aed2_phyto_pars.nml}.
Required if \code{aed2_phytoplankton} is active.}

\item{sim_par}{Character; path to \code{simstrat.par}-style JSON config.}

\item{out_dir}{Character; directory to which inflow \code{.dat} files
will be written.}

\item{levels}{Numeric vector; inflow depths (m). If \code{NULL} together
with \code{surf_flow}, a single surface inflow at 0 m is assumed.}

\item{surf_flow}{Logical vector; TRUE for surface inflows, FALSE for deep
inflows. Must be the same length as \code{levels}.}

\item{default_value}{Numeric; default initial inflow value (0 for most
tracers).}

\item{unit}{Character; default unit string to use for non-pH variables.}

\item{inflow_map}{Data frame mapping AED2 modules to inflow variable base
names; defaults to [aed2_inflow_map].}
}
\value{
Invisibly returns the character vector of inflow variable names
  for which files were written (without \code{.dat} extension).
}
\description{
Creates template Simstrat inflow \code{.dat} files for all AED2 variables
associated with the active modules in \code{aed2_file}. Phytoplankton
variables are expanded based on group names in \code{aed2_phyto_pars.nml}.
}
\details{
By default, all inflows are treated as surface inflows at 0 m, and all
variables are initialised with \code{default_value}, except
\code{CAR_pH_inflow}, which is initialised to 8 with unit \code{"pH"}.
}
