% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ensemble_wq.r
\name{run_ensemble_wq}
\alias{run_ensemble_wq}
\title{Run an ensemble of LakeEnsemblR.WQ models selected}
\usage{
run_ensemble_wq(
  config_file,
  models = c("GLM-AED2", "GOTM-WET", "GOTM-Selmaprotbas", "SIMSTRAT-AED2"),
  folder = ".",
  parallel = FALSE,
  ncores = NULL,
  verbose = TRUE,
  on_error = c("skip", "stop"),
  validate = TRUE,
  post_process = FALSE
)
}
\arguments{
\item{config_file}{Character. Path to master LakeEnsebmlerWQ.yaml configuration file}

\item{models}{Character vector. Models to be run (e.g. c("GLM-AED","GOTM-WET","GOTM_SELMA","SIMSTRAT-AED2")).}

\item{folder}{Character. Base folder containing model run directories}

\item{parallel}{Logical. Run in parallel (not implemented yet).}

\item{ncores}{Integer. Number of cores (not used yet).}

\item{verbose}{Logical. Print progress messages.}

\item{on_error}{Character. "skip" (default) skips invalid/failed models with warnings,
"stop" aborts on first invalid/failed model.}

\item{validate}{Logical. If TRUE, run model-specific validation first. This may be important to prevent getting error on a later stage}

\item{post_process}{Logical. If TRUE, call your harmonization/post-processing on successful runs. (not integrated yet)}
}
\value{
A list containing validation results, run results, and NetCDF paths for successful runs.
}
\description{
Runs selected models via their own runner packages, and optionally skipping invalid
models with a warning, then optionally post-processes the produced NetCDF outputs.
}
