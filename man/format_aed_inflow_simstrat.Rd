% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_simstrat_input.r
\name{format_aed_inflow_simstrat}
\alias{format_aed_inflow_simstrat}
\title{Format a Simstrat-style AED2 inflow file (character vector)}
\usage{
format_aed_inflow_simstrat(
  varname,
  levels,
  surf_flow,
  sim_par = "simstrat.par",
  times = NULL,
  default_value = 0,
  unit = "millimolesPerMeterCubed"
)
}
\arguments{
\item{varname}{Character; AED2 inflow variable name, e.g. \code{"PHS_frp_inflow"}.}

\item{levels}{Numeric vector; inflow depths (m), one per branch.}

\item{surf_flow}{Logical vector; same length as \code{levels}, TRUE for
surface inflows, FALSE for deep inflows.}

\item{sim_par}{Character; path to \code{simstrat.par}-style JSON config.}

\item{times}{Numeric vector of times [days] for the template time series.
If \code{NULL}, \code{Simulation$"Start d"} and \code{Simulation$"End d"}
are used with a daily step.}

\item{default_value}{Numeric; default value to assign at all depths and times
(e.g. 0 for most tracers, 8 for \code{CAR_pH_inflow}).}

\item{unit}{Character; unit string to include in the header, e.g.
\code{"millimolesPerMeterCubed"} or \code{"pH"}.}
}
\value{
A character vector of lines to be written to a \code{.dat} file.
}
\description{
This helper creates the contents of a Simstrat inflow file for a single
AED2 variable, given a time axis and simple inflow geometry (deep vs
surface inflows). It mirrors the structure of \code{format_flow_simstrat()}
used for Q/T/S inflows, but is applied to AED2 tracers.
}
\seealso{
[generate_simstrat_aed2_inflows()]
}
\keyword{internal}
