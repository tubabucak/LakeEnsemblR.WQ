# Sets up the model-specific configuration files

Sets up the model-specific config files with the right groups

## Usage

``` r
set_up_configs(config_file, folder = ".")
```

## Arguments

- config_file:

  character; name of LakeEnsemblR_WQ config file

- folder:

  path; location of config_file
