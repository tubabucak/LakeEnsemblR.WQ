# Set coupling settings for each model

Sets any coupling settings that are needed to run a model

## Usage

``` r
set_coupling(config_file, folder)
```

## Arguments

- config_file:

  character; name of LakeEnsemblR_WQ config file

- folder:

  path; location of config_file
